# web — where the suite drives. Unset, Playwright starts `dev:client` on this port itself.
BASE_URL=http://localhost:5173
# mock (default, CI) answers every /api call from the contract handlers; live talks to a real server (local only)
E2E_API_MODE=mock
