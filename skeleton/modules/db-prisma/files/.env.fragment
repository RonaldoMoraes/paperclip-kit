DATABASE_URL=postgresql://postgres:postgres@localhost:5432/__DB_NAME__   # <- apps/server/src/database/database.config.ts (the server's sole reader; the Prisma CLI and the seed read the same key through db/prisma/database-url.ts; `wt run --db` overrides it per worktree)
