# Yarn 4 through corepack (`packageManager` in package.json pins the version) — no vendored release.
nodeLinker: node-modules

# Postinstall scripts run: msw copies its worker into apps/web/public, husky installs the git hooks.
enableScripts: true
