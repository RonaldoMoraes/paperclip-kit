/**
 * The settings screen's copy. The screen itself is thin — the version line and whatever
 * actions the opted-in modules contribute — so these are its title, the line it shows
 * when no module has put anything there, and the version line. PLACEHOLDER WORDING; the
 * keys are what web and mobile share.
 */
export const SETTINGS_COPY = {
  title: "Settings",
  version: "Version {version}",
  noActions: "Nothing to manage yet.",
};
